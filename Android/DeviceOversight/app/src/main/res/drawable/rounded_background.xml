<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#A88ADF" /> <!-- Màu nền -->
    <corners android:radius="20dp" /> <!-- Bán kính góc bo tròn -->
    <stroke
        android:width="2dp"
        android:color="@color/blue" /> <!-- Viền và màu viền -->
</shape>
